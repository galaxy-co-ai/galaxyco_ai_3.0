#!/usr/bin/env sh

# NOTE:
# We no longer update AI_CONTEXT.md on every commit because it:
# - makes the working tree perpetually dirty
# - encourages "docs(status)" spam commits
#
# Instead, AI_CONTEXT.md is updated and auto-committed on push
# (see .husky/pre-push).

exit 0
