#!/usr/bin/env sh

# Intentionally disabled.
#
# We keep docs/status/AI_CONTEXT.md updated on main via GitHub Actions
# (.github/workflows/ai-context.yml) so merges done on GitHub stay accurate
# without creating local commit noise.

exit 0
