#!/usr/bin/env sh

# Auto-update AI_CONTEXT.md after each commit
# Just update the file, don't auto-commit (prevents infinite loop)
npm run update-context

echo ""
echo "✅ AI_CONTEXT.md updated (not committed)"
echo "   Run 'git add docs/status/AI_CONTEXT.md && git commit --amend --no-edit' to include in this commit"
echo "   Or commit it separately later"
